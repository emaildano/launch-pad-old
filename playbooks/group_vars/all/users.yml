# ==============================================================================
# Users
# ==============================================================================

web_user: web
web_group: www-data

users:
  - name: "{{ web_user }}"
    groups:
      - "{{ web_group }}"
  - name: dolson
    groups:
      - sudo
      - "{{ web_group }}"
  - name: sbaer
    groups:
      - sudo
      - "{{ web_group }}"
